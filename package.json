{
  "name": "project-template",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf public/scripts && tsc",
    "dev": "rm -rf public/scripts && tsc -p tsconfig-dev.json",
    "start": "rm -rf public/scripts && tsc && http-server -p 3000",
    "lint": "eslint src --ext .js --ext .ts",
    "lint-fix": "eslint src --ext .js --ext .ts --fix"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.4.0",
    "@typescript-eslint/parser": "^5.4.0",
    "eslint": "^8.2.0",
    "http-server": "^14.0.0",
    "typescript": "^4.5.2"
  }
}
